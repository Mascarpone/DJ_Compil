/*
 * Ce test décrit la fonctionnalité suivante du compilateur :
 *      La fonction reduce doit prendre un tableau non vide en paramètre
 *
 * Le résultat attendu de la compilation est :
 *
 *
 * Le résultat attendu de l'exécution est :
 *      La valeur retournée par reduce() n'est pas spécifiée.
 *      Il n'y a cependant pas de segfault.
 *
 */

int foo(int a, int b)
{
    return 0;
}

int main()
{
    int[] T;
    int i = reduce(foo, T);
    printint(i);
    return 0;
}
