/*
 * Ce test décrit la fonctionnalité suivante du compilateur :
 *      Bon fonctionnement des opérations courante sur les entiers
 *
 * Le résultat attendu de la compilation est :
 *      Pas d'erreur.
 *
 * Le résultat attendu de l'exécution est :
 *      Exécution avec un code de retour égal à 12
 */

int main()
{
    int a = 2;
    int b = 10;
    return (a+b);
}
